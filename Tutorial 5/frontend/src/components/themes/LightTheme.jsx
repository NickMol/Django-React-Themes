import { createTheme } from "@mui/material/styles";

const LightTheme = createTheme({
    palette: {
        mode:"light",
        primary:{
            main: "#EE4B2B"
        }, 
        secondary:{
            main: "#7933ff"
        }
    
    }

})

export default LightTheme